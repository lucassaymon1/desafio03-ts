import styled from "styled-components";

export const Container = styled.div`
  button{
    padding-inline: 15px;
    padding-block: 10px;
    background-color: #f44336;
    border-radius: 12px;

    color: white;
    font-weight: 600;
  }
`